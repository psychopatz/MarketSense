-- MS_StaticCatalog.lua
-- Static seed catalog intentionally empty. MarketSense now relies on the
-- modular flat classifier and runtime rule exports for generated stock.

MarketSense = MarketSense or {}
MarketSense.StaticCatalog = MarketSense.StaticCatalog or {}

return MarketSense.StaticCatalog
